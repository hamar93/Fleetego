{
  "name": "fleetego-agent-static",
  "version": "1.0.0",
  "scripts": {
    "dev": "live-server .",
    "build": "echo 'Static site - no build needed'",
    "deploy": "netlify deploy --prod --dir ."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}